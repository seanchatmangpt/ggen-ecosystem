# Receipts

`receipts/` contains committed ecosystem-level evidence such as the bootstrap manufacturing receipt. Runtime ggen receipts may also be produced under `.ggen-v2/` during an execution.

A valid receipt binds the exact subject identity, admitted graph/manufacturer identities, authority, consequences or output hashes, replay coordinates, and scoped standing. A file named `receipt` is not evidence unless its matching verifier accepts it.
