# WS4 invariant 40: release asset checksum verification remains strict
ASK { <https://ggen.io/ecosystem/github-sync#install> <http://seanchatmangpt.github.io/packs/github-actions#runCommand> ?v . FILTER(CONTAINS(STR(?v), "sha256sum --check --strict")) }
