# WS4 invariant 11: successful manufacture does not grant write authority
ASK { <https://ggen.io/ecosystem/github-sync#workflow> <http://seanchatmangpt.github.io/packs/github-actions#standingEffect> ?v . FILTER(CONTAINS(LCASE(STR(?v)), "read-only deterministic manufacture")) }
