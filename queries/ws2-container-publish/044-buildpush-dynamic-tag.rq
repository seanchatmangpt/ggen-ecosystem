PREFIX gha: <http://seanchatmangpt.github.io/packs/github-actions#>
PREFIX ex: <https://ggen.io/ecosystem/github-sync#>
ASK {
  ex:containerBuildPush gha:withParam ?p . FILTER(CONTAINS(STR(?p), "inputs.image_tag") && CONTAINS(STR(?p), "github.ref_name"))
}
